/*
The angle between two vectors can be calculated by finding
the dot product of the vectors divided by the product of the length of the vectors,
and taking the arcocsine of that value.

//  float myAngle =  acos( (PVector.dot(a,b))/(a.mag()*b.mag()));
*/

//float angleBetweenVectors(PVector a, PVector b){
//  //  float myAngle =  acos( (PVector.dot(a,b))/(a.mag()*b.mag()));
//  float pAngle = PVector.angleBetween(a,b);

//  return myAngle;
  
//}
